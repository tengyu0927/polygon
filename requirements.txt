# 核心推理不需要任何第三方库（模型 JSON 里只有权重，纯标准库可跑）。
# 以下都是"有则更快/更准，无则自动降级"：
numpy>=1.24        # 训练提速几十倍；缺失时走纯 Python 实现，结果一致
scikit-learn>=1.3  # 岭回归+GBM 融合的 GBM 部分。缺失时 predict_mos 自动降级为
                   # 纯岭回归并打警告（D+1 会从 1.00 退到 1.08）
